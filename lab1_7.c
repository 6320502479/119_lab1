#include <stdio.h>
#include <math.h>
int main()
{
 float time;
 int min,l;
  scanf("%f",&time);
  l=floor(time);
  min=(time-l)*100;
   if(time>=12.00)
   {
   }
   if(time<=11.59)
   {
   }
   printf("%d",min);
}
